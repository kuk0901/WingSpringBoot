<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.edu.wing.category.dao.CategoryDao">
    <select id="allCategorySelectList" resultType="map">
        SELECT 'MINUS' as category, category_no, category_name
        FROM minus_category
        WHERE category_no != 1
        UNION ALL
        SELECT 'PLUS' as category, category_no, category_name
        FROM plus_category
        WHERE category_no != 1
    </select>
</mapper>